import { RainbowWritingDirective } from './rainbow-writing.directive';

describe('RainbowWritingDirective', () => {
  it('should create an instance', () => {
    const directive = new RainbowWritingDirective();
    expect(directive).toBeTruthy();
  });
});
